def detect_portfolio_overlap(holdings: list) -> int:
    """
    Scans holdings for duplicate high-concentration stocks (e.g., TCS, Infosys).
    Returns a percentage of overlap.
    """
    # In a real implementation, this would compare ISINs across mutual funds
    # For the hackathon contract:
    return 45
